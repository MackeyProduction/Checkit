<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.cbc.azubiproject.checkit">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/checkit_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/checkit_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".GroupViewActivity"
            android:theme="@style/checkit_app_theme" />
        <activity
            android:name=".GroupActivity"
            android:theme="@style/checkit_app_theme" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name=".RegisterActivity" />
        <activity
            android:name=".ProfileActivity"
            android:theme="@style/checkit_app_theme" />
        <activity
            android:name=".QuestionModeActivity"
            android:theme="@style/checkit_app_theme" />
    </application>

</manifest>